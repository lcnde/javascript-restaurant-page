import './style.css';

import { pageOneLoad } from './page-1-load';

pageOneLoad();

//make a function that will run to set the button onclick events. every button on click should trigger first a function that wipes the page, second a function that reconstructs it with the page loader functions

